const LoadingScreen = () => {
  return (
    <div className="bg-bg flex justify-center items-center w-full h-full">
      <h1 className="text-text-muted">Loading...</h1>
    </div>
  );
};

export default LoadingScreen;
